#include <iostream>
#include <fstream>

using namespace std;

// declararea fisierelor
// fin da replace la cin
// fout da replace la cout
ifstream fin("sum.in");
ofstream fout("sum.out");

int main()
{
    int lista[100], n,m;
    fin>> n;
    for(int i=0;i<n;i++){
        fin>> lista[i];
    }
    fin.close();
    for(int i=0;i<n;i++){
        fout<< lista[i];
    }
    fout.close();

    return 0;
}
